# Qwen-TTS validation requirements
# Note: torch 2.9.1 + torchaudio 2.9.1 are installed separately in Dockerfile
#       via --index-url https://download.pytorch.org/whl/cu130 (ARM64 cu130 wheels).
#       Do NOT list them here to avoid pip resolving from PyPI.

# TTS
qwen-tts==0.1.1

# qwen-tts pins transformers==4.57.3; explicit here for reproducibility
transformers==4.57.3
accelerate>=0.26.0

# Voice cloning: transcription (ARM64: runs on CPU, triton not available)
openai-whisper

# Audio I/O
soundfile
